[color]
    ui = true
[github]
    user = phiwer
[core]
    editor = emacs

[includeIf "gitdir:~/sources/aosp/icup/**"]
    path = ~/.gitconfig-work
    
[includeIf "gitdir:~/.dotfiles.git"]
    path = ~/.gitconfig-dots
